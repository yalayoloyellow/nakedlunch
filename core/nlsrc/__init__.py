# Код резки и хранения корпуса, внесённый из CLI-проекта nakedlunch
# (~/nakedlunch/core/{store,cutter,generator}.py) 2026-08-01.
#
# ПОЧЕМУ внесён, а не импортируется по пути, как раньше: приложение — это новая
# версия nakedlunch и обязано жить самостоятельно. Пока эти три файла читались
# из ~/nakedlunch через sys.path, каталог CLI был скрытой рантайм-зависимостью:
# переименуй или убери его — и корпус пропадал бы вместе с ним, хотя сами данные
# лежат совсем в другом месте (~/Documents/nakedlunch/data, 526 МБ).
#
# Внесены ровно три файла — это всё замыкание: cutter и generator дальше ничего
# из CLI не тянут. Изменены ровно две строки импорта в store.py (core.* → .*).
# Данные не тронуты и лежат там же, где лежали: CLI и приложение по-прежнему
# читают один и тот же корпус, если CLI кому-то понадобится.
